import * as React from "react";
import { Box } from "@mui/material";
import { NewsCardProps } from "../Pages/Home";

const NewsCard: React.FC<NewsCardProps> = ({ data }) => {
  return <Box>NewsCard</Box>;
};

export default NewsCard;
